# prometheus.yml
global:
  scrape_interval: 15s # Adjust as needed

scrape_configs:
  - job_name: 'weaviate'
    static_configs:
      - targets: ['weaviate:2112'] # 'weaviate' is the service name from docker-compose.yml